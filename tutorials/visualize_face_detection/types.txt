
node UploadVideo:
    events:
        uploadedVideo(videoFile: BinaryData)

node SliceVideosByFrames:
    handlers:
        processVideo(videoFile: BinaryData)
    events:
        releaseFrames(frames: list(Image))

node MapList:
    handlers:
        execute(list: list(object))
        receiveProcessedItem(obj: object)
    events:
        processedItem(item: object)
        processedAllItems(items: list(object))

node FaceDetectionNetwork:
    handlers:
        processInputImage(img:Image)
    events:
        processedImage(bounding_boxes: list(tuple(float, float, float, float)))


